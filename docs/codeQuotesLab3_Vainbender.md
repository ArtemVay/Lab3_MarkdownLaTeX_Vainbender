Блоки кода
Встроенный (inline) код:
Команда `git status` показывает состояние репозитория.
Многострочный код:
```bash
print("Hello, Markdown")
```
Цитаты
Цитата создаётся символом >:
>Это цитата в Markdown.
>Вложенная цитата:
>Первый уровень
>>Второй уровень

пример команды Git:
```bash
git init
```
Блок кода:
```csharp
int x = 21;
Console.WriteLine({}, x);
```
```
str s = "Hi";
Console.WriteLine({}, s);
```
> Обычная цитата

> И вложенная
>>цитата